import styled from 'styled-components'

export const Container = styled.header({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    minWidth: '100vw',
    padding: '0 1rem',
})
  